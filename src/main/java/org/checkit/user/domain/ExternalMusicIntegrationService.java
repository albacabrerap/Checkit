/*
package org.checkit.user.domain;

import org.checkit.user.domain.User;
import org.springframework.stereotype.Service;

@Service
public class ExternalMusicIntegrationService {

    public String generateEmbeddedPlaybackLink(User user) {
        String platform = "SPOTIFY"; // Mock pull
        String fallbackTrack = */
/*link a spotify*//*
; // Lofi Focus

        return switch (platform.toUpperCase()) {
            case "SPOTIFY" -> */
/*link a spotify*//*
 + fallbackTrack;
            case "YOUTUBE" -> */
/*link a youtubr*//*
;
            case "SOUNDCLOUD" -> */
/*link a soundcloud*//*
;
            default -> fallbackTrack;
        };
    }
}*/
